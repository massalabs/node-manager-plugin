export enum networks {
  mainnet = 'mainnet',
  buildnet = 'buildnet',
}

export const ENV = {
  DEV: 'dev',
  TEST: 'test',
  MASSASTATION: 'massastation',
  STANDALONE: 'standalone',
};

export const DEFAULT_JSON_RPC_PORT = 33035;
export const DEFAULT_GRPC_PORT = 33037;

export const DEFAULT_RPC_NAME = 'local node';
